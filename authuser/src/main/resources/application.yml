spring:
  application:
    name: ead-authuser
  config:
    import: 'configserver:'
  cloud:
    config:
      discovery:
        service-id: ead-config-server
      username: configserver
      password: 'ead123cs'
      authority: 'ROLE_ADMIN'